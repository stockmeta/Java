import java.util.Scanner;
public class Salary {
	public static void main (String[]args) {
	Scanner in = new Scanner(System.in);
	
	boolean bonus,noOfServiceYears,totalSales;
	double additionalBonus;
	System.out.println("Enter amount:" );
	
	if (noOfServiceYears <= 5);
		bonus = 10 * (noOfServiceYears); 
			if (bonus = 20 * (noOfServiceYears));
	
	else if (totalSales < 5000);
		if ((additionalBonus) = 0); 
	
	else if ((totalSales >= 5000) &&  totalSales < 10000);
			additionalBonus = totalSales * (0.03); 
				if (additionalBonus = totalSales * (0.06));
			
	System.out.println("Your Paycheck is:" + (baseSalary) + (bonus) + (additionalBonus));
}
}







	
